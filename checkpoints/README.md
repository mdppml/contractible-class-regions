# Checkpoints

This folder stores optional intermediate mesh checkpoints.

Checkpoints are only written when `--checkpoint-every-level` is enabled. They can be useful for debugging long runs or inspecting intermediate surface states.

Generated checkpoint files are ignored by git.
